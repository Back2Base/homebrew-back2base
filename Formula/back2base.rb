# typed: false
# frozen_string_literal: true

# This file was generated by GoReleaser. DO NOT EDIT.
class Back2base < Formula
  desc "Containerized Claude Code with curated MCP servers"
  homepage "https://back2base.net"
  version "0.53.1"
  license "MIT"

  depends_on "docker" => :optional

  on_macos do
    if Hardware::CPU.intel?
      url "https://github.com/back2base/back2base-dist/releases/download/v0.53.1/back2base_darwin_amd64.tar.gz"
      sha256 "bccc3296cd875cca633f66af7b82a40882bea12e304a0ff9205cf289fbdce13f"

      define_method(:install) do
        bin.install "back2base"
      end
    end
    if Hardware::CPU.arm?
      url "https://github.com/back2base/back2base-dist/releases/download/v0.53.1/back2base_darwin_arm64.tar.gz"
      sha256 "7447a4d691dc311e2f815f873218be1a5e16c15fd1d398347c08c5ea0b981a96"

      define_method(:install) do
        bin.install "back2base"
      end
    end
  end

  on_linux do
    if Hardware::CPU.intel? && Hardware::CPU.is_64_bit?
      url "https://github.com/back2base/back2base-dist/releases/download/v0.53.1/back2base_linux_amd64.tar.gz"
      sha256 "d2847346971c73d9d0fa26557d6a817e68664d0ad8a817ddd92474aa91d304ec"
      define_method(:install) do
        bin.install "back2base"
      end
    end
    if Hardware::CPU.arm? && Hardware::CPU.is_64_bit?
      url "https://github.com/back2base/back2base-dist/releases/download/v0.53.1/back2base_linux_arm64.tar.gz"
      sha256 "4fad170d85449ebf0e3be6942af85693b2689709f9f6322e3dfe66382f48ee84"
      define_method(:install) do
        bin.install "back2base"
      end
    end
  end

  def post_install
    # Best-effort base-image pre-pull so the first `back2base run` doesn't
    # have to fetch ~500MB on a cold start. Then prune older base/compose
    # images so the user doesn't accumulate multi-GB drift across upgrades.
    # Both steps are skipped silently when docker isn't installed or
    # reachable — never fails the brew install.
    if which("docker")
      system "docker", "pull", "ramseymcgrath/back2base-base:latest"
      system "#{bin}/back2base", "prune", "--yes", "--quiet"
    end
  end

  def caveats
    <<~EOS
      back2base launches a containerized Claude Code session.

      First run pulls the base image (~500MB) and builds a thin local
      layer on top. To pre-fetch / pre-build ahead of time:

        docker pull ramseymcgrath/back2base-base:latest
        back2base build

      Sign in to enable cloud sync + memory MCP:

        back2base login
    EOS
  end

  test do
    system "#{bin}/back2base", "version"
  end
end
